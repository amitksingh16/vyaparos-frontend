1. TECH STACK: Frontend (vercel: vyaparos-frontend.vercel), Backend (Railway: vyaparos-backend-production), Database (SQLite/Postgres), Auth (Firebase Phone).
2. CURRENT BLOCKER: Firebase 'auth/api-key-not-valid' error on live domain.
3. MISSION: Fix OTP flow, ensure reCAPTCHA is working with the new Google Cloud Site Key, and verify that the backend is receiving requests from Netlify (CORS check).
4. FUTURE GOAL: Subscription model with Razorpay and 'is_premium' logic in Users table.